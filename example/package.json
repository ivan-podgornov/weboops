{
    "private": true,
    "name": "weboops-example",
    "scripts": {
        "build": "weboops --mode build --filesHashingEnabled=false",
        "dev": "weboops --mode development"
    },
    "dependencies": {
        "@weboops/core": "*"
    }
}
